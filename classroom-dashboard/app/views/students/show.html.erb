<!-- individual student's page -->
<nav class="navbar navbar-default">
	<div class="container">

    <ul class="nav navbar-nav navbar-left">
      <a href="#"><%= link_to "Flashcards", section_flashcards_path(@section)%></a>
      <a href="#"><%= link_to "Memory Game", section_quiz_path(@section)%></a>
    </ul>

		<div class="navbar-header">
      <%= link_to @section.course + " " + @section.semester, section_path(@section), class: "navbar-brand" %>
      <!-- <h1>
          <%= @section.course %>
          <%= @section.semester %>
      </h1> -->
    </div>

		<ul class="nav navbar-nav navbar-right">
      <div style="text-align:right">
        <p class = add_student><%= link_to "Add Student", new_section_student_path(@section)%></p>
      </div>
    </ul>
	</div>
</nav>



<div style="text-align:center">
  <%= image_tag @student.student_img.url(:student_index) %>
  <br>
  <%= @student.firstName %>
  <%= @student.lastname %>
  <br>
  <%= @student.pronouns %>
  <br>
  <%= @student.school %>
  <br>
  <%= @student.info %>
  <br>

  <%= link_to "Edit/Delete", edit_section_student_path %>
</div>
