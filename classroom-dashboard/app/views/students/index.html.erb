<!-- student list page at section/:section_id/students -->
<!-- fix this better form? -->
<h1>Students</h1>

<h5><%= link_to "Back to Course", section_path(@section)%></h5>

<div style="text-align:center">
  <p><%= link_to "Add Student", new_section_student_path(@section)%></p>
</div>

<%= render @section.students.all.order("lastname ASC") %>
