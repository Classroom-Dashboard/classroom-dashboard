<!-- loaded in calls to render @student for randomization and student list-->

<div class="col-md-3">
  <a href="<%= @section.id %>/students/<%= student.id %>">
    <%= image_tag student.student_img.url(:student_index) %>
    <h1><%= link_to student.firstName, section_student_path(student.section_id, student) %></h1>
  </a>
</div>
