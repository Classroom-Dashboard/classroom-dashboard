<br>
<br>
<br>
<br>
<!-- fix this to better form once we meet with client -->

<!-- form for new and edit, loaded in new.html.erb -->
<!-- Fields prompted to the user to create a new Section -->
<%= simple_form_for @section do |f| %>
	<%= f.input :course, label: "Course (ex: CS5)" %>
	<%= f.input :semester, label: "Semester (ex:S2018)" %>
	<br>
	<%= f.button :submit %>
<% end %>
