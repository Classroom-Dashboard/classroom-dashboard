<nav class="navbar navbar-default">
	<div class="container">

    <ul class="nav navbar-nav navbar-left">
      <a href="#"><%= link_to "Flashcards", section_flashcards_path(@section)%></a>
      <a href="#"><%= link_to "Memory Game", section_quiz_path(@section)%></a>
    </ul>

		<div class="navbar-header">
      <%= link_to @section.course + " " + @section.semester, section_path, class: "navbar-brand" %>
      <!-- <h1>
          <%= @section.course %>
          <%= @section.semester %>
      </h1> -->
    </div>

		<ul class="nav navbar-nav navbar-right">
      <div style="text-align:right">
        <p class = add_student><%= link_to "Add Student", new_section_student_path(@section)%></p>
      </div>
    </ul>
	</div>
</nav>

<h1> Random Student </h1>

<div style="text-align:center">
  <% @student =  @section.students.all.order("RANDOM()").limit(1)[0] %>
  <% if @student != nil then %>
  <%= image_tag @student.student_img.url(:student_index) %>
  <br>
  <%= @student.firstName%>
  <%= @student.lastname %>
  <% end %>


</div>

<h3 style="text-align: center"><%= link_to "Generate Random Student!", section_flashcards_path(@section)%></h3>
